spring:
  application:
    name: config-client
  cloud:
    config:
      profile: dev
#      discovery:
#        enabled: true
#        service-id: config-server
      label: master
      name: appconfig
      url: http://localhost:8888
    bus:
      trace:
        enabled: true
